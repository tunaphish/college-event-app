extends layout

block content
  h1= title
  
  ul
  each event in event_list
    - var url = '/event/' + event.eventID
    li 
      a(href=url) #{event.name} 
  else
    li There are no events.
    
  
