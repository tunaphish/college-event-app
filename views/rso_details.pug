extends layout

block content
  h1 #{title}: #{rso[0].name}
  
  p #[strong Active:] #{rso[0].active}
  p #[strong University:] #{rso[0].uniName}
  
  if error
    p= error
  else
    form(method='POST' action='')
      button.btn.btn-primary(type='submit') Join
