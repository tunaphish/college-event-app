extends layout

block content
  h1= title
  
  ul
  each rso in rso_list
    - var url = '/RSO/' + rso.rsoID
    li
      a(href=url) #{rso.name} 
      | (#{rso.name})

  else
    li There are no RSOs.
